# Hanzo CI/CD for hanzoai/mds: .github/workflows/cicd.yml imports hanzoai/ci,
# which reads this file. The build door builds each image, one BuildKit Job
# per platform.
images:
  - name: mds
    context: .
    dockerfile: Dockerfile
    repo: ghcr.io/hanzoai/mds
    platforms: [linux/amd64]
